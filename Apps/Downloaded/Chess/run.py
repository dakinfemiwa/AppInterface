import tkinter
from tkinter imoprt *
import random

class run:
    def __init__(self, AppScreen, AppName, width, height):
        self.AppScreen = AppScreen
        self.AppName = AppName
        self.screenWidth = width
        self.screenHeight = height
        print(self.AppName)


